# Ship Report

## 概况
- 时间：2026-03-28 05:32:55
- 耗时：约 11 分钟
- 分支：dev
- Diff 范围：last 10 commits (bd28e55..c24d365)
- Tag：无

## Gate 通过情况

| Gate | 状态 | 摘要 |
|------|------|------|
| 1. Review | ✅ (修复后) | 0 Critical, 1 Major (已修复), 3 Minor, 3 Nit |
| 2. Test | ✅ | 补充 5 个测试，发现 1 个缺陷 |
| 3. Bench | ✅ | FIX framer 22.5ns, BQ throughput 4.6ns — 无退化 |
| 4. Doc | ⏭️ | 内部重构，跳过文档更新 |
| 5. Git | ✅ | 3 个 commit，已 push |

## Review 发现

### Major（已修复）
- `EvictingQueueBytesStats::throughput()` 在 delta snapshot 上使用绝对 `last_pop_id` 而非差值 → 已修复并添加测试

### Minor（记录）
- `begin_string_` 未 JSON 转义（实践中安全，建议后续修复）
- `ErrorEnum` 名称对非错误枚举（TransportEvent/State）有歧义（建议后续重命名为 NamedEnum）
- `EvictingQueueBytesStats` 缺少 throughput/loss_rate 测试（已补充）

### Nit（记录）
- `json_escape` 在 eph-core 中仍用 `eph::net::detail` namespace
- `connection_error_name` 返回 `const char*` 与 `error_name()` 返回 `string_view` 不一致（已修复）
- `FixSessionConfig::validate()` 不校验 begin_string 格式

## 新增测试
| 测试 | 覆盖目标 |
|------|----------|
| `EvictingQueueBytesStats.throughput_on_delta_snapshot` | delta snapshot 上 throughput 使用差值 |
| `EvictingQueueBytesStats.throughput_zero_duration_returns_zero` | 零时长边界条件 |
| `EvictingQueueBytesStats.loss_rate_no_overwrite_returns_zero` | 无丢失时 loss_rate |
| `EvictingQueueBytesStats.loss_rate_with_overwrite` | 有丢失时 loss_rate |
| `EvictingQueueBytesStats.loss_rate_no_pushes_returns_zero` | 零推送边界条件 |

## Benchmark 对比

| Benchmark | 当前 | 判定 |
|-----------|------|------|
| BM_FixFramerDecode | 22.5ns | ✅ 持平 (历史 ~22ns) |
| BM_BoundedQueue_ZeroCopy_Throughput | 4.6ns | ✅ 持平 |

## 提交记录
| Commit | Message |
|--------|---------|
| 9f4b820 | fix(containers): correct EvictingQueueBytesStats::throughput() on delta snapshots |
| b26f583 | refactor(core): unify error_name return types to string_view |
| c24d365 | test(net): adapt EXPECT_STREQ to EXPECT_EQ for string_view returns |

## 后续建议
- 考虑将 `ErrorEnum` 重命名为 `NamedEnum`（覆盖非错误枚举）
- `eph::net::detail::json_escape` namespace 应迁移到 `eph::core::detail`
- `FixSessionConfig::validate()` 应校验 begin_string 格式（"FIX."/"FIXT." 前缀）
- `begin_string_` 在 `MessageView::to_json()` 中应做 JSON 转义
