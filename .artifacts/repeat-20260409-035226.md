# Repeat Report — bench latency rewrite

## 概况
- 命令：`/design [until: plan-bench-latency-rewrite-20260409-023700.md 全部 6 阶段已完成且通过验收]`
- 模式：直接执行（无 batch-until → 主循环自身调用 /design）
- 开始：2026-04-09 03:02:00
- 结束：2026-04-09 03:52:26
- 总轮数：6（有效 6，无效 0）
- 终止原因：until 条件满足 — plan 6 阶段全部 commit 并通过 acceptance

## 各轮详情

── 第 1 轮 ✅ 5693263 · 耗时 ~3m ──
phase 1 — wipe latency bench, lay new skeleton
 35 files | 4 insertions / 4381 deletions
 (删除 27 个旧 latency 文件 + bench_common.hpp + bench_latency.sh + xmake target；
  建立 6 个空目录骨架带 .gitkeep；xmake build 通过)

── 第 2 轮 ✅ 3991dd9 · 耗时 ~25m ──
phase 2 — core/ + mock/lib/ shared header layer
 25 files | 2192 insertions / 4 deletions
 (9 个 core/ + 7 个 mock/lib/ header；5 个单测 cpp + xmake.lua；
  28 tests passing, 1 host-skipped (no SMT sibling on aarch64))

── 第 3 轮 ✅ 1df2314 · 耗时 ~30m ──
phase 3 — tcp / udp / ws scenarios + 12 build targets
 16 files | 1317 insertions / 4 deletions
 (3 scenarios × {client, scenario, mock, bench} + 12 xmake targets；
  loopback smoke test 全 4-leg 报告非零，--server-work-ns 1000 → SRV ≈ 1µs)
 注：发现与 eph-dpdk/eph-net 内部 bench 命名冲突，target 加 lat_ 前缀

── 第 4 轮 ✅ d6dc0ee · 耗时 ~30m ──
phase 4 — exchange (quant) scenarios + 10 build targets
 8 files | 967 insertions / 0 deletions
 (3 scenarios + 2 mocks 共享 stream_scheduler 与 ws/client 帮助类；
  market p50=7µs, order inflight 1→16 p99 6.5×, mock 30k frames/sec)

── 第 5 轮 ✅ 67cb348 · 耗时 ~10m ──
phase 5 — bench_latency.sh thin orchestrator
 1 file | 685 insertions / 0 deletions
 (--loopback / --nic-* 双模式；6 场景 loopback 完整跑通 ~20s；
  脚本不创建任何文件；trap EXIT INT TERM 干净清理 mock/netns/dpdk)

── 第 6 轮 ✅ c7800c6 · 耗时 ~6m ──
phase 6 — regression sanity-check + memory cleanup
 1 file | 108 insertions / 0 deletions
 (kernel loopback baseline vs old NIC wire — 数值减半符合预期；
  D-5 work_spin 验证 SRV 0→260ns；memory 标记完成；retro 写入 .artifacts/)

## 汇总表格

| 轮次 | 状态 | commit  | 摘要 |
|------|------|---------|------|
| 1 | ✅ | 5693263 | phase 1 — wipe latency bench, lay new skeleton |
| 2 | ✅ | 3991dd9 | phase 2 — core/ + mock/lib/ shared header layer |
| 3 | ✅ | 1df2314 | phase 3 — tcp / udp / ws scenarios + 12 build targets |
| 4 | ✅ | d6dc0ee | phase 4 — exchange (quant) scenarios + 10 build targets |
| 5 | ✅ | 67cb348 | phase 5 — bench_latency.sh thin orchestrator |
| 6 | ✅ | c7800c6 | phase 6 — regression sanity-check + memory cleanup |

## 累计变更

```
git diff 5693263~1..c7800c6 --shortstat
 76 files changed, 5835 insertions(+), 4227 deletions(-)
```

净增 1608 行（新代码 + 测试 + 脚本 + 文档减去删除的旧代码）。

## 关键产物

- **Plan**: `.artifacts/plan-bench-latency-rewrite-20260409-023700.md`（19 个决策点）
- **Retrospective**: `.artifacts/bench-rewrite-retro-20260409.md`（数据对比 + 偏差说明）
- **Memory**: `~/.claude/projects/.../memory/project_bench_rewrite.md`（标记完成 + 三个非显然 fact）

## 与 plan 的偏差（在 commit 中已记录）

1. **二进制命名前缀 `lat_`** — plan 写 `mock_<scenario>` / `bench_<scenario>`，但与
   `eph-dpdk/benchmarks/bench_udp.cpp` 和 `eph-net/benchmarks/bench_ws.cpp` 自动 glob
   的 target 冲突。phase 3 link 时发现，全部加 `lat_` 前缀。
2. **DPDK 实现是 POSIX stub** — plan 阶段 3/4 acceptance 只要求 "12/10 targets build pass"。
   `*_dpdk` 目标 link eph-dpdk 并定义 `EPH_USE_DPDK=1`，但源码仍走 POSIX
   并在 startup 输出 stderr warning。真正的 DPDK transport 实现是后续工作。

## 后续建议

- 真正实现 DPDK transport（eph-dpdk Platform / EAL / `rte_eth_*` 分别用于 raw TCP / UDP / WS），
  作为 follow-up plan
- 真实 NIC + isolcpus 上跑一次完整 benchmark 收集 production baseline（本 session 仅 loopback 验证）
- `mock_lat_exchange_ws --trace FILE` 接口位 → 实现 Binance trace 回放
