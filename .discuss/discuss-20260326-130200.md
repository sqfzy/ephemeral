# Discussion Record

## Context
- 时间：2026-03-26 13:02:00
- 耗时：约 6 分钟
- 用户原始需求：改进很好。现在从代码设计层面考虑。改进的实现方式是否足够优雅？
- 复杂度评估：中
- 讨论轮数：3 轮
- 参与角色：R1(风险卫士), R2(极简主义者), R3(性能狂热者), R6(维护性倡导者), R14(架构师)

## 内容摘要

核心争议：process_ws_data 中内联 rx_enqueue 绕过了 deliver_message 的安全检查（UTF-8 校验、on_message 回调、drop 处理）。R1 指出三个具体 bug，R2 指出 stats 有两套维护路径。R3 为性能辩护但被 R14 反驳（noexcept 函数大概率内联）。最终收敛为 deliver_message 添加 `defer_stats` 参数方案：保留所有安全检查，仅跳过 per-frame stats 更新，循环后批量 flush。

---

## 第 1 轮

- R1: 内联 enqueue 绕过 UTF-8 校验、on_message 回调、drop 处理
- R2: stats 有两套路径（内联局部累加 vs deliver_message per-frame 原子操作）
- R3: deliver_message 职责过多应分层，内联是合理的调用处拆分
- R6: process_ws_data 已膨胀为上帝函数，stats 在两条路径间割裂
- R14: kLastOnlyDeliver 和 deliver-all 应统一为 batch 模式

## 第 2 轮

- R1: 具体指出 on_message 回调语义不一致、BoundedQueue drop 未处理、HWM 采样缺失
- R2: 提出 skip_stats 参数方案——所有帧走 deliver_message，仅跳过 stats
- R3: 担心函数调用链开销（~5-8ns/帧），提出 rx_enqueue_fast()
- R6: 联盟 R2，接受 skip_stats；R3 被说服放弃内联
- R14: 确认 noexcept 函数内联消除调用开销

## 第 3 轮（收敛）

全员同意：回滚内联 enqueue，deliver_message 新增 defer_stats 参数。

## 最终方案

1. 回滚 process_ws_data 中的内联 rx_enqueue + 局部 stats 累加
2. deliver_message 新增 `bool defer_stats = false` 参数
3. process_ws_data 在 batch 路径传 defer_stats=true，循环后 flush stats
4. 保留其他优化（produce()、ns_per_cycle 缓存、批量 record_rx_latency、seqlock store-release）
