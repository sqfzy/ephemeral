/// @file bench_config.hpp
/// Forwarding shim — content moved to framework/bench_config.hpp.
/// Kept temporarily so existing scenarios compile during refactor phases 0-3.
/// Will be deleted in phase 4.
#pragma once
#include "framework/bench_config.hpp"
